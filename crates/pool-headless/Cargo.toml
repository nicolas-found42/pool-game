[package]
name = "pool-headless"
description = "The headless CLI (architecture.md §7): replay a recorded match, compute one shot"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[[bin]]
name = "pool-headless"
path = "src/main.rs"

[dependencies]
pool-sim.workspace = true
pool-rules.workspace = true
pool-match.workspace = true
# `pool-ai` joins this list with `pipe` (§7's third mode): its payload is the AI's encoding and lands
# in M4, where the crate lands.
clap.workspace = true
serde = { workspace = true }
serde_json.workspace = true

[lints]
workspace = true
