[package]
name = "pool-match"
description = "Match layer: the Session drive loop, per-rack seed derivation, and the input-log types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
pool-sim.workspace = true
pool-rules.workspace = true
pool-rng.workspace = true
# The execution-noise perturbation's aim rotation is the pinned `libm` escape hatch of
# `architecture.md` §3 — a declaration conversion, never part of the evolution.
libm.workspace = true
serde = { workspace = true }
serde_json.workspace = true

[dev-dependencies.jsonschema]
version = "0.33"

[lints]
workspace = true
